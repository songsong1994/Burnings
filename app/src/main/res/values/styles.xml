<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="home">
        <item name="android:background">@null</item>
        <item name="android:drawableTop">@drawable/selector_home</item>
        <item name="android:textColor">@color/selector_color</item>
        <item name="android:button">@null</item>
    </style>
    <style name="series">
        <item name="android:background">@null</item>
        <item name="android:drawableTop">@drawable/selector_side</item>
        <item name="android:textColor">@color/selector_color</item>
        <item name="android:button">@null</item>
    </style>
    <style name="my">
        <item name="android:background">@null</item>
        <item name="android:drawableTop">@drawable/selector_my</item>
        <item name="android:textColor">@color/selector_color</item>
        <item name="android:button">@null</item>
    </style>

</resources>
